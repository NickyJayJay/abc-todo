{
    "name": "abc-todo",
    "version": "1.0.0",
    "scripts": {
        "dev": "nodemon server.js",
        "build": "cd client && npm run build",
        "start": "cd client && npm run preview"
    },
    "devDependencies": {
        "nodemon": "^3.1.10"
    },
    "dependencies": {
        "dotenv": "^16.5.0",
        "express": "^5.1.0",
        "mongoose": "^8.14.2"
    }
}
